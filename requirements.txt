fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy[asyncio]==2.0.35
alembic==1.14.1
pydantic[email]==2.12.5
pydantic-settings==2.5.2
python-dotenv==1.0.1

# Database drivers
psycopg2-binary==2.9.10  # For Alembic migrations (sync)
asyncpg==0.30.0          # For async PostgreSQL in runtime
aiosqlite==0.20.0        # For async SQLite (development only)

# Firebase authentication
firebase-admin==6.5.0

# AWS S3 storage
boto3==1.35.0
